# ============================================================
# 统一推理环境依赖
# 环境: CUDA 11.8 + Python 3.11, PyTorch 2.4.1
# ============================================================
#
# 安装步骤:
#
#   1. 安装 PyTorch (CUDA 11.8):
#      pip install torch==2.4.1 torchvision==0.19.1 \
#          --index-url https://download.pytorch.org/whl/cu118
#
#   2. 安装其余依赖:
#      pip install -r unified_requirements.txt
#
#   3. pyradiomics 需从源码编译，加 --no-build-isolation:
#      pip install --no-build-isolation "pyradiomics>=3.0.1,<3.1"
#
# ------------------------------------------------------------

# --- 图像处理 ---
Pillow>=9.0.0
opencv-python-headless>=4.5.0,<5  # <5: opencv 5.x 要求 numpy>=2，与 pyradiomics 冲突
albumentations>=1.4.0

# --- 科学计算 ---
numpy>=1.24.0,<2                  # <2: pyradiomics 3.0.1 不兼容 numpy 2.x C API
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.2.0
matplotlib>=3.7.0

# --- 深度学习模型库 ---
timm>=1.0.0
safetensors>=0.3.0
pretrainedmodels>=0.7.4
efficientnet-pytorch>=0.7.1
huggingface-hub>=0.24.0

# --- 预训练模型加载 ---
open_clip_torch>=2.24.0        # infer_biomedclip
transformers>=4.45.0,<4.51    # infer_medsiglip/infer_biomedclip (<4.51 规避 CVE-2025-32434 torch.load 强制检查)
sentencepiece>=0.1.99         # infer_medsiglip (SiglipTokenizer)
protobuf>=3.20.0              # infer_medsiglip (SiglipTokenizer 解析 spiece.model)

# --- Agent (infer_seg_agent / infer_cls_agent) ---
openai>=1.0.0                 # LLM 客户端（local_gpt_oss 和云端 API 共用）
# openai-harmony              # 可选: GPT-OSS Harmony 协议解析（未安装时自动 fallback 到 apply_chat_template）

# --- 配置管理 ---
hydra-core>=1.3.0              # infer_medsam2
omegaconf>=2.3.0               # infer_medsam2
iopath>=0.1.9                  # infer_medsam2
ml-collections>=1.1.0          # infer_transunet
PyYAML>=6.0                    # run_all.py (config.yaml)

# --- 医学图像评估 ---
medpy>=0.5.2                   # infer_transunet (PyPI 最新为 0.5.2，支持 Python 3.11)

# --- AutoGluon 分类 (infer_autogluon) ---
# autogluon 完整安装约 2-3GB，推理只需 tabular 子包
# 如 predictor 训练时用了 xgboost/catboost 等后端，改为 autogluon.tabular（完整表格版）
autogluon.tabular[light]>=1.0  # 轻量版（仅 lightgbm），推理足够
pyradiomics>=3.0.1,<3.1       # 3.1.0 sdist metadata 有 bug；需 --no-build-isolation 安装
SimpleITK>=2.3
openpyxl>=3.0                  # (可选) Excel 支持

# --- 工具 ---
tqdm>=4.64.0
